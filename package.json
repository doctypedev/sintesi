{
  "name": "sintesi-monorepo-root",
  "version": "0.0.0",
  "private": true,
  "description": "Root package for the Sintesi monorepo",
  "scripts": {
    "build": "pnpm -r build && if [ $? -eq 0 ]; then echo \"✅ Build Completed. Run: $(pwd)/packages/cli/dist/cli/src/index.js\"; else echo \"❌ Build Failed! Please check the errors above.\"; fi",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r clean",
    "release": "pnpm changeset version && pnpm changeset publish",
    "generate:sidebar": "tsx docs/scripts/generateSidebar.ts",
    "docs:dev": "pnpm run generate:sidebar && vitepress dev docs",
    "docs:build": "pnpm run generate:sidebar && vitepress build docs",
    "docs:preview": "pnpm run generate:sidebar && vitepress preview docs"
  },
  "pnpm": {
    "supportedArchitectures": {
      "os": [
        "darwin",
        "linux",
        "win32"
      ],
      "cpu": [
        "arm64",
        "x64"
      ]
    }
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.8",
    "@types/vue": "^1.0.31",
    "glob": "^13.0.0",
    "tsx": "^4.19.2",
    "vite": "^5.0.10",
    "vitepress": "^1.6.4",
    "vue": "^3.5.25"
  },
  "packageManager": "pnpm@8.15.5"
}
